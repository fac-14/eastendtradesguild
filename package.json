{
  "name": "eastendtradesguild",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:5000/",
  "dependencies": {
    "Express": "^3.0.1",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "2.0.4"
  },
  "scripts": {
    "start": "node server.js",
    "start:client": "react-scripts start",
    "dev": "concurrently --kill-others-on-fail \"npm start\" \"npm run start:client\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "test:coverage": "npm test -- --coverage",
    "coverage": "codecov",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "codecov": "^3.1.0",
    "concurrently": "^4.0.1",
    "eslint-config-prettier": "^3.1.0",
    "prettier": "^1.14.3",
    "react-testing-library": "^5.2.0",
    "supertest": "^3.3.0"
  }
}
